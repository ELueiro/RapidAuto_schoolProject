@model IEnumerable<RapidAuto.MVC.Models.Commande>

@{
    ViewData["Title"] = "Index";
}

<h1>Commander une véhicule</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserNom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vehicule.Id ) de voiture
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserNom)
            </td>
           <td>
                @Html.DisplayFor(modelItem => item.Vehicule.Id)
            </td>
            <td>
                @Html.ActionLink("Modifier", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Détails", "Details", new { id=item.Id  }) |
                @Html.ActionLink("Supprimer", "Delete", new { id=item.Id  })
            </td>
        </tr>
}
    </tbody>
</table>
